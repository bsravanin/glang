#/usr/sh

rm src/*.java
ant -buildfile build.xml
mv build/jar/gramola.jar lib


if [ ! -d "bin" ]; then
   mkdir bin
fi

if [ ! -d "db" ]; then
   mkdir db
fi


if [ ! -d "dotdata" ]; then
   mkdir dotdata
fi


if [ ! -d "export" ]; then
   mkdir export
fi

